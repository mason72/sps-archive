import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "New Event — Pixeltrunk",
};

export default function NewEventLayout({ children }: { children: React.ReactNode }) {
  return children;
}
